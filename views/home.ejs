<%- include('partials/header') -%>
<h1>Home</h1>
<p><%= content %> </p>
<% if (posts) {
    posts.forEach((post) => {%>
        <h1><%= post.title%></h1>
        
        <p><%= post.body.substr(0,99) + "..." %> <a href= "<%= "/posts/" + post._id %>" >Read more</a></p>
        
        <%})}%>
<%- include('partials/footer') -%>